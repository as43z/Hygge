{
  "name": "engine",
  "version": "1.0.0",
  "description": "Game Engine for HTML browser games",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "tsc",
    "compact:browser": "rollup -c Build/rollup-browser.config.js --bundleConfigAsCjs",
    "build:browser": "npm run transpile && npm run compact:browser"
  },
  "keywords": [
    "game-engine",
    "game-dev"
  ],
  "author": "as43z",
  "license": "MIT",
  "dependencies": {
    "rollup": "^4.14.0"
  }
}
